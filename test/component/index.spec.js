// for code coverage purposes, import the root component for the application
// s.t. we can load all the used component code so that coverage is included
// for source which doesn't have any tests yet.
import {Session} from '../../src/components/session'
